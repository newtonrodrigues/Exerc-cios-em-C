#include <stdio.h>
#include <stdlib.h>

int main(){

   int i, j, matriz[12][12];

    for(i=0; i<=0; i++){
        for(j=0; j<=0; j++){
            matriz[i][j] = 1;
            printf("[%d][%d]: %d", i, j, matriz[i][j]);
        }
    }
   return 0;
}
